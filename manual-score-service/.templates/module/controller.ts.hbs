import { catchAsync } from "#utils/index";
import { {{camelCase name}} } from "./{{kebabCase name}}";


export const controller = {
	// This is a dummmy function. Please edit this function to suit your needs
	// You can add more functions to this controller
	// You can also remove this function if you do not need it

    dummyFunction: catchAsync(async (req, res) => {
		const response = await {{camelCase name}}.dummyFunction(req.params, req.body, res.locals.user, req.query);
		res.jsend.success(response, "Something happened successfully");
	}),
};