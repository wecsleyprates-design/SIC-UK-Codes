-- Drop table rel_banking_verifications if it exists
DROP TABLE IF EXISTS integration_data.rel_banking_verifications;

-- Drop the enum type verification_status_enum if it exists
DROP TYPE IF EXISTS verification_status_enum;