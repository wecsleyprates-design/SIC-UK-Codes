-- Drop table core_giact_response_codes if it exists
DROP TABLE IF EXISTS integrations.core_giact_response_codes;

-- Drop the enum type verification_type_enum if it exists
DROP TYPE IF EXISTS verification_type_enum;
